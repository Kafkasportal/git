## 2024-05-22 - [Accessibility & Localization Polish]
**Learning:** Hardcoded English strings in accessible elements (like `sr-only` "Close") are easy to miss but critical for screen reader users in a localized app.
**Action:** Always check `sr-only` content in base UI components for localization when working in a non-English codebase.
