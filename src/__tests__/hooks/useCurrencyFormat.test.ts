/**
 * useCurrencyFormat Tests
 */

import { describe, it, expect } from "vitest";

describe("useCurrencyFormat", () => {
  it("should pass - placeholder test", () => {
    expect(true).toBe(true);
  });
});
