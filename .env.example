# ============================================
# Dernek Yönetim Sistemi - Environment Variables
# ============================================
# 
# Kullanım:
# 1. Bu dosyayı kopyalayın: cp .env.example .env.local
# 2. Değerleri kendi gerçek değerlerinizle değiştirin
# 3. .env.local dosyası git'e commit edilmemelidir (.gitignore'da olmalı)
#
# NOT: NEXT_PUBLIC_ prefix'i olan değişkenler client-side'da kullanılabilir
#      NEXT_PUBLIC_ olmayan değişkenler sadece server-side'da kullanılabilir
# ============================================

# ============================================
# Application Configuration
# ============================================
NEXT_PUBLIC_APP_NAME="Dernek Yönetim Sistemi"
NEXT_PUBLIC_APP_VERSION="0.1.0"
NEXT_PUBLIC_ENABLE_REALTIME="true"
NEXT_PUBLIC_ENABLE_ANALYTICS="false"

# ============================================
# Appwrite Configuration (Required)
# ============================================
# Appwrite endpoint (client-side accessible)
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1

# Appwrite Project ID (client-side accessible)
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your-project-id

# Appwrite Database ID (client-side accessible)
NEXT_PUBLIC_APPWRITE_DATABASE_ID=your-database-id

# Appwrite API Key (SERVER-SIDE ONLY - Do NOT add NEXT_PUBLIC_ prefix!)
APPWRITE_API_KEY=your-appwrite-api-key

# Appwrite Site ID (SERVER-SIDE ONLY - Optional, for Appwrite Sites deployment)
APPWRITE_SITE_ID=your-site-id

# ============================================
# Security Configuration (Required for Production)
# ============================================
# CSRF Secret - Minimum 32 characters (Required in production)
CSRF_SECRET=your-csrf-secret-minimum-32-characters-long

# Session Secret - Minimum 32 characters (Required in production)
SESSION_SECRET=your-session-secret-minimum-32-characters-long

# ============================================
# Email Configuration (Optional)
# ============================================
# SMTP settings for email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=noreply@example.com

# ============================================
# SMS Configuration (Optional)
# ============================================
# Twilio settings for SMS notifications
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# Rate Limiting Configuration (Optional)
# ============================================
# Default rate limit: 100 requests per 15 minutes
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# ============================================
# File Upload Configuration (Optional)
# ============================================
# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Maximum number of files per upload (default: 5)
MAX_FILES_PER_UPLOAD=5

# ============================================
# Analytics (Optional)
# ============================================
# Google Analytics Measurement ID (format: G-XXXXXXXXXX)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================
# BrowserStack Configuration (Optional - for cross-browser testing)
# ============================================
# BrowserStack credentials for automated cross-browser testing
# Get your credentials from: https://www.browserstack.com/accounts/settings
BROWSERSTACK_USERNAME=your-browserstack-username
BROWSERSTACK_ACCESS_KEY=your-browserstack-access-key

# ============================================
# Development Only
# ============================================
# NOTE: NODE_ENV is automatically set by Next.js
# Do NOT override it in .env.local
# NODE_ENV=development  # ❌ Don't set this manually
